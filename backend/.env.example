# Переменные окружения для Backend (NIST стандарт: защита конфигурации)
# Скопируйте в .env и заполните значения

# Database (MySQL 8.0)
DATABASE_URL="mysql://news_user:news_password@localhost:3306/news_app"

# JWT Secret (минимум 256 бит для HS256)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-256-bits"
JWT_EXPIRES_IN="7d"

# Server
PORT=3000
HOST="0.0.0.0"
NODE_ENV="development"

# CORS
CORS_ORIGIN="http://localhost:5173"

# Bcrypt (OWASP рекомендует 10-12 раундов)
BCRYPT_ROUNDS=12
