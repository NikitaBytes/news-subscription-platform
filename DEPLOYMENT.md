# 🚀 Быстрое развёртывание News Subscription App

## Развёртывание в 2 команды

### Шаг 1: Соберите фронтенд

```bash
cd frontend
npm install
npm run build
cd ..
```

### Шаг 2: Запустите всё в Docker

```bash
docker-compose up -d
```

**Готово!** Приложение доступно по адресу: **http://localhost**

---

## 📋 Что включено?

- **Frontend**: React + TypeScript (Nginx на порту 80)
- **Backend**: Fastify + Prisma (Node.js на порту 3000)
- **Database**: PostgreSQL 16 (порт 5432)
- **Admin Panel**: Adminer (порт 8080)

---

## 🔐 Первый вход

### Тестовые пользователи (создаются автоматически):

**Администратор:**

- Email: `admin@news.app`
- Пароль: `admin123`

**Редактор:**

- Email: `editor@news.app`
- Пароль: `editor123`

**Подписчик:**

- Email: `user@news.app`
- Пароль: `user123`

---

## 🌐 URL адреса

- **Приложение**: http://localhost
- **Backend API**: http://localhost:3000/api
- **Health Check**: http://localhost:3000/health
- **Adminer (БД)**: http://localhost:8080
  - Система: PostgreSQL
  - Сервер: `db`
  - Пользователь: `news_user`
  - Пароль: `news_password_2024`
  - База: `news_app`

---

## 🛠️ Полезные команды

### Запуск и остановка

```bash
# Запустить все сервисы
docker-compose up -d

# Остановить все сервисы
docker-compose down

# Остановить и удалить данные
docker-compose down -v
```

### Логи

```bash
# Все логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f db
```

### Перезапуск

```bash
# Пересобрать и запустить заново
docker-compose up -d --build

# Перезапустить конкретный сервис
docker-compose restart backend
```

### База данных

```bash
# Вход в PostgreSQL контейнер
docker exec -it news_postgres psql -U news_user -d news_app

# Бэкап БД
docker exec news_postgres pg_dump -U news_user news_app > backup.sql

# Восстановление БД
docker exec -i news_postgres psql -U news_user news_app < backup.sql
```

---

## 🔧 Разработка

### Локальная разработка (без Docker)

**Backend:**

```bash
cd backend
npm install
cp .env.example .env
# Отредактируйте .env для локальной БД
npm run dev
```

**Frontend:**

```bash
cd frontend
npm install
npm run dev
```

### Применение миграций БД

```bash
cd backend
npx prisma migrate dev --name migration_name
```

### Заполнение БД тестовыми данными

```bash
cd backend
npx prisma db seed
```

---

## 🔒 Безопасность (для продакшена)

### Обязательно изменить:

1. **JWT Secret** в `docker-compose.yml`:

```yaml
JWT_SECRET: "ваш_случайный_секрет_минимум_32_символа"
```

2. **Пароль БД** в `docker-compose.yml`:

```yaml
POSTGRES_PASSWORD: "надёжный_пароль"
DATABASE_URL: "postgresql://news_user:ваш_надёжный_пароль@db:5432/news_app"
```

3. **CORS Origin** для вашего домена:

```yaml
CORS_ORIGIN: "https://yourdomain.com"
```

### Генерация безопасного JWT секрета:

```bash
openssl rand -base64 32
```

---

## 📦 Структура проекта

```
.
├── backend/
│   ├── src/              # Исходный код API
│   ├── prisma/           # Схема БД и миграции
│   ├── Dockerfile        # Docker образ backend
│   └── .env.production   # Переменные окружения
├── frontend/
│   ├── src/              # React приложение
│   └── dist/             # Собранные статические файлы
├── docker-compose.yml    # Оркестрация контейнеров
├── nginx.conf            # Конфигурация веб-сервера
└── DEPLOYMENT.md         # Этот файл
```

---

## 🐛 Решение проблем

### Порт 80 уже занят

```bash
# Измените порт фронтенда в docker-compose.yml
ports:
  - "8081:80"  # Вместо 80:80
```

### Backend не стартует

```bash
# Проверьте логи
docker-compose logs backend

# Проверьте подключение к БД
docker-compose exec backend npx prisma db pull
```

### Ошибка миграции БД

```bash
# Пересоздайте БД
docker-compose down -v
docker-compose up -d
```

### Frontend показывает 404

```bash
# Пересоберите фронтенд
cd frontend
npm run build
cd ..
docker-compose restart frontend
```

---

## 📊 Мониторинг

### Проверка статуса сервисов

```bash
docker-compose ps
```

### Использование ресурсов

```bash
docker stats
```

### Health check

```bash
curl http://localhost:3000/health
# Должно вернуть: {"status":"ok","timestamp":"..."}
```

---

## 🚀 Production deployment

### С использованием Docker Swarm или Kubernetes

1. Настройте reverse proxy (Traefik/Nginx)
2. Добавьте SSL сертификаты (Let's Encrypt)
3. Настройте автоматические бэкапы БД
4. Используйте внешнюю БД (AWS RDS, DigitalOcean)
5. Настройте мониторинг (Prometheus + Grafana)

### Рекомендации:

- Используйте `.env` файлы для secrets
- Настройте автоматические бэкапы
- Включите логирование в внешний сервис
- Используйте CDN для статики
- Настройте rate limiting
- Включите HTTPS

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все порты свободны
3. Перезапустите контейнеры: `docker-compose restart`
4. Пересоберите с нуля: `docker-compose up -d --build`

---

## ✅ Чеклист перед деплоем

- [ ] Собран фронтенд (`npm run build`)
- [ ] Изменён JWT_SECRET в production
- [ ] Изменён пароль БД
- [ ] Настроен CORS_ORIGIN для вашего домена
- [ ] Проверены healthcheck endpoints
- [ ] Настроены бэкапы БД
- [ ] Добавлен SSL сертификат (для HTTPS)
- [ ] Протестированы все функции
- [ ] Настроен мониторинг и алерты