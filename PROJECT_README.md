# Веб-приложение "Подписка на новости"

## 📋 Описание проекта

Полнофункциональное веб-приложение для управления подписками на новости с системой аутентификации, авторизации на основе ролей и комплексным логированием.

### Технологический стек

**Backend:**

- Fastify 4.x (веб-фреймворк)
- TypeScript 5.x
- Prisma ORM + MySQL 8.0
- JWT аутентификация (@fastify/jwt)
- bcrypt (хеширование паролей, OWASP стандарт)
- helmet (OWASP security headers)
- Zod (валидация данных)
- Pino (логирование)

**Frontend:**

- React 18.3
- TypeScript 5.x
- Vite 5.x (сборщик)
- React Router 6.x
- Axios (HTTP клиент)
- Zustand (state management)

**Инфраструктура:**

- Docker + Docker Compose (MySQL 8.0)
- Adminer (опционально для управления БД)

### Безопасность

Реализованы стандарты **OWASP** и **NIST**:

- Пароли хешируются с помощью bcrypt (12 раундов)
- JWT токены для аутентификации
- Helmet для HTTP security headers
- CORS политика
- Role-Based Access Control (RBAC)
- Валидация всех входных данных
- Логирование действий пользователей и ошибок

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ и npm
- Docker и Docker Compose

### 1. Запуск MySQL в Docker

```bash
docker-compose up -d
```

Это запустит:

- MySQL 8.0 на порту 3306
- Adminer на порту 8080 (опционально, для просмотра БД через браузер)

### 2. Настройка Backend

```bash
cd backend

# Установка зависимостей
npm install

# Генерация Prisma клиента
npm run prisma:generate

# Применение миграций БД
npm run prisma:migrate

# Заполнение начальными данными (роли, категории, админ)
npm run prisma:seed

# Запуск в режиме разработки
npm run dev
```

Backend запустится на `http://localhost:3000`

**Тестовый админ:**

- Email: `admin@example.com`
- Пароль: `admin123`

### 3. Настройка Frontend

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

Frontend запустится на `http://localhost:5173`

## 📁 Структура проекта

### Backend (`/backend`)

```
backend/
├── src/
│   ├── config/           # Конфигурация (env, constants)
│   ├── db/               # Prisma клиент
│   ├── plugins/          # Fastify плагины (jwt, cors, helmet)
│   ├── routes/           # API роуты
│   ├── controllers/      # Контроллеры (бизнес-логика)
│   ├── services/         # Сервисный слой
│   ├── middlewares/      # Middleware (auth, rbac, error)
│   ├── schemas/          # Zod схемы валидации
│   ├── types/            # TypeScript типы
│   ├── utils/            # Утилиты (hash, jwt)
│   └── server.ts         # Точка входа
├── prisma/
│   ├── schema.prisma     # Схема БД
│   └── seed.ts           # Начальные данные
├── .env                  # Переменные окружения
└── package.json
```

### Frontend (`/frontend`)

```
frontend/
├── src/
│   ├── api/              # API клиент (axios)
│   ├── components/       # React компоненты
│   │   ├── common/       # Общие (Header, ProtectedRoute)
│   │   ├── auth/         # Аутентификация
│   │   ├── news/         # Новости
│   │   └── admin/        # Админ панель
│   ├── pages/            # Страницы
│   ├── context/          # React Context (Auth)
│   ├── types/            # TypeScript типы
│   ├── utils/            # Утилиты
│   ├── App.tsx           # Главный компонент
│   └── main.tsx          # Точка входа
└── package.json
```

## 🗄️ База данных

### Схема (MySQL 8.0)

Реализованы все требования задания:

- **users** - пользователи
- **roles** - роли (Admin, Editor, Subscriber)
- **user_roles** - связь many-to-many
- **categories** - категории новостей
- **news** - новостные статьи
- **subscriptions** - подписки пользователей
- **user_action_logs** - логи действий пользователей
- **app_error_logs** - логи ошибок приложения
- **http_error_logs** - логи HTTP ошибок

### Роли и права доступа

1. **ROLE_SUBSCRIBER** (по умолчанию при регистрации):

   - Просмотр новостей
   - Управление своими подписками

2. **ROLE_EDITOR**:

   - Все права Subscriber +
   - Создание/редактирование/удаление новостей

3. **ROLE_ADMIN**:
   - Все права Editor +
   - Управление пользователями (изменение ролей, статусов)
   - Просмотр логов (действия, ошибки)

## 📡 API Endpoints

### Аутентификация (`/api/auth`)

- `POST /register` - регистрация
- `POST /login` - вход
- `POST /logout` - выход
- `GET /me` - текущий пользователь

### Новости (`/api/news`)

- `GET /` - список новостей (с фильтром по категории)
- `GET /:id` - одна новость
- `POST /` - создать (Editor/Admin)
- `PUT /:id` - обновить (Editor/Admin)
- `DELETE /:id` - удалить (Editor/Admin)

### Категории (`/api/categories`)

- `GET /` - список категорий

### Подписки (`/api/subscriptions`)

- `GET /my` - мои подписки
- `POST /` - подписаться
- `DELETE /:categoryId` - отписаться

### Пользователи (`/api/users`) - Admin only

- `GET /` - список пользователей
- `PUT /:id/role` - изменить роль
- `PUT /:id/status` - изменить статус (активен/неактивен)

### Логи (`/api/logs`) - Admin only

- `GET /user-actions` - логи действий
- `GET /http-errors` - логи HTTP ошибок
- `GET /app-errors` - логи ошибок приложения

## 🔐 Безопасность

### Реализованные меры (OWASP/NIST)

1. **Пароли**: bcrypt с 12 раундами (OWASP рекомендация)
2. **Аутентификация**: JWT с секретным ключом мин. 256 бит
3. **HTTP Headers**: Helmet для защиты от XSS, clickjacking и др.
4. **CORS**: настроенная политика для Frontend
5. **Валидация**: Zod схемы для всех входных данных
6. **RBAC**: строгая авторизация на основе ролей
7. **Логирование**: audit trail для всех критичных действий
8. **Error Handling**: централизованная обработка с логированием

## 🧪 Тестирование

### Сценарий тестирования

1. **Регистрация**: создайте нового пользователя
2. **Вход**: войдите под админом (admin@example.com / admin123)
3. **Просмотр новостей**: откройте страницу новостей
4. **Создание новости**: создайте новость (требуется роль Editor/Admin)
5. **Подписки**: подпишитесь на категорию
6. **Админ панель**: просмотр пользователей и логов (требуется роль Admin)

## 📝 Примечания

- Все пароли хешируются перед сохранением в БД
- JWT токены хранятся в localStorage (в production лучше использовать httpOnly cookies)
- Логи можно просматривать через Adminer или API endpoints
- Для production необходимо изменить JWT_SECRET в `.env`

## 🛠️ Разработка

### Полезные команды

**Backend:**

```bash
npm run dev          # Запуск с hot-reload
npm run build        # Сборка TypeScript
npm run prisma:studio # Открыть Prisma Studio
```

**Frontend:**

```bash
npm run dev          # Запуск с hot-reload
npm run build        # Сборка для production
npm run preview      # Просмотр production сборки
```

## 📄 Лицензия

GNU GPL v3 (General Public License v3)
