# 📰 News Subscription App

Современное веб-приложение для подписки на новости с красивым UI

## 🚀 Быстрый старт (2 команды)

### Автоматическое развёртывание:

```bash
./deploy.sh
```

### Или вручную:

**Шаг 1:** Соберите фронтенд

```bash
cd frontend && npm install && npm run build && cd ..
```

**Шаг 2:** Запустите в Docker

```bash
docker-compose up -d
```

**Готово!** Откройте http://localhost

---

## 🌟 Возможности

- ✅ Аутентификация с JWT
- ✅ Управление новостями (CRUD)
- ✅ Система подписок на категории
- ✅ Три роли: Админ, Редактор, Подписчик
- ✅ Современный UI/UX
- ✅ Темная тема
- ✅ Адаптивный дизайн
- ✅ Полное логирование (NIST стандарты)

---

## 🔐 Тестовые аккаунты

| Роль          | Email           | Пароль    |
| ------------- | --------------- | --------- |
| Администратор | admin@news.app  | admin123  |
| Редактор      | editor@news.app | editor123 |
| Подписчик     | user@news.app   | user123   |

---

## 🛠️ Технологии

**Frontend:**

- React 18 + TypeScript
- Vite
- Framer Motion (анимации)
- CSS Modules

**Backend:**

- Fastify (Node.js)
- Prisma ORM
- PostgreSQL 16
- JWT Authentication
- TypeScript

**DevOps:**

- Docker + Docker Compose
- Nginx
- Multi-stage builds

---

## 📖 Документация

- [Полная инструкция по развёртыванию](DEPLOYMENT.md)
- [История обновлений](UPDATES.md)
- [Postman коллекция](News_Subscription_API.postman_collection.json)

---

## 🌐 URL адреса

После запуска доступны:

- **Приложение**: http://localhost
- **API**: http://localhost:3000
- **Adminer (БД)**: http://localhost:8080

---

## 📋 Полезные команды

```bash
# Логи всех сервисов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Полная пересборка
docker-compose up -d --build
```

---

## 🔒 Безопасность

Перед продакшн деплоем обязательно измените:

- `JWT_SECRET` в docker-compose.yml
- Пароль БД (`POSTGRES_PASSWORD`)
- `CORS_ORIGIN` на ваш домен

Генерация секрета:

```bash
openssl rand -base64 32
```

---

## 📦 Структура

```
├── backend/          # Fastify API
├── frontend/         # React SPA
├── docker-compose.yml
├── nginx.conf
├── deploy.sh         # Скрипт развёртывания
└── DEPLOYMENT.md     # Подробная инструкция
```

---

## 🐛 Проблемы?

1. Проверьте логи: `docker-compose logs -f`
2. Перезапустите: `docker-compose restart`
3. Пересоберите: `docker-compose up -d --build`
4. См. [DEPLOYMENT.md](DEPLOYMENT.md) для решения частых проблем

---

## 📄 Лицензия

GNU GPL v3 (General Public License v3)
